# helm-template render fixture (see tests/README.md).
# Expected: the render FAILS, because the gateway rule would have no port.
nameOverride: mcp
application:
  containerPort: 8001
  containerPortName: http
service:
  enabled: true
  targetPortName: htpp
httpRoute:
  enabled: true
networkPolicy:
  enabled: true
