# Scripts

Helper scripts for gitlab cicd jobs.

```
.
├── gitlab-diff-mr-comment-template.sh  # Script to format a message in collapsible format in a gitlab merge request comment
├── README.md                           # This README file
└── render-charts.sh                    # script to render helm charts with value files
```
